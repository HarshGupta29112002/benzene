# Copy this file to .env and fill the values before running the backend locally
# Do NOT commit your real credentials to version control.

# MySQL configuration
DB_HOST=localhost
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=your_database_name

# Server configuration
PORT=5000
NODE_ENV=development

# JWT secret for signing tokens
JWT_SECRET=613b412020f806d2eac0e6d1845f74991a5d42b2b0da50cc665a5e5b27a85a6e

# Frontend/admin URLs (used for CORS in production)
CLIENT_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174

# (Optional) Cloud storage config for uploads
# S3_BUCKET=your_bucket_name
# S3_REGION=your_bucket_region
# S3_ACCESS_KEY_ID=your_access_key
# S3_SECRET_ACCESS_KEY=your_secret_key
